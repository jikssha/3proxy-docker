version: '3.8'

services:
  3proxy:
    build: .
    container_name: 3proxy-socks5
    ports:
      # 映射端口 - 根据需要修改
      - "40000:40000"
    environment:
      # 可选：指定固定端口（如果不设置，容器内会随机生成）
      - PORT=40000
    restart: unless-stopped
    # 可选：限制资源使用
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.5'
    #       memory: 256M
    #     reservations:
    #       cpus: '0.25'
    #       memory: 128M
    # 可选：使用自定义网络
    # networks:
    #   - proxy-network

# 可选：定义自定义网络
# networks:
#   proxy-network:
#     driver: bridge